import "CameraRGBDSimple.idsl";
module RoboCompDepthEstimation
{
    sequence<byte> DepthValueType;
    
    struct DepthScene
    {
     
        DepthValueType value;
        int width;
        int height;
        int depth;
    };
    
    interface DepthEstimation
    {
        idempotent void getDepthEstimation(RoboCompCameraRGBDSimple::TImage image, out DepthScene result);
    };
};
